<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:iOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    iOS:Page.UseSafeArea="True"
    x:Class="XamarinFormsFirebase.Views.RegistrationView.SignUpPage"
    Title="{x:Binding Title}">

    <ContentPage.Content>
        <StackLayout Margin="10,50">
            <StackLayout>
                <Label Text="{x:Binding Title}"/>
            </StackLayout>
            <StackLayout Spacing="20">
                <Entry Text="{x:Binding EmailId}" Placeholder="Enter Email Id"/>
                <Entry Text="{x:Binding Password}" Keyboard="Numeric" Placeholder="Password"/>
                <Button Text="Sign Up" Command="{x:Binding SignUpCommand}"/>
                <Button Text="Log In" Command="{x:Binding LogInCommand}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>

