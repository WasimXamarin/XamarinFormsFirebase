<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:iOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    iOS:Page.UseSafeArea="True"
    x:Class="XamarinFormsFirebase.Views.RegistrationView.LogInPage"
    Title="{x:Binding Title}">

    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Label Text="{x:Binding Title}"/>
            </StackLayout>
            <StackLayout>
                <Entry Text="{x:Binding EmailId}" Placeholder="Enter Email Id"/>
                <Entry Text="{x:Binding Password}" Placeholder="Password" Keyboard="Numeric"/>
                <Button Text="Log In" Command="{x:Binding LogInCommand}"/>
                <Button Text="Sign Up" Command="{x:Binding SignUpCommand}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>

